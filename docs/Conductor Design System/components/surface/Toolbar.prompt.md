Control strip inside a panel — run controls, filters, composer actions.

```jsx
<Toolbar align="between" divider="top">
  <SegmentedControl value="local" options={["Local", "Cloud"]} />
  <Button variant="primary" icon="play">Run Test</Button>
</Toolbar>
```
