The smallest state signal in the system. Live states get a soft halo ring; idle/offline gets none.

```jsx
<StatusDot state="connected" label="R9QYC01EMXL" />
<StatusDot state="running" pulse />
```

Needs the `cd-pulse` keyframe on the page when `pulse` is set.
