Shortcut capsules, one per key.

```jsx
<Kbd>⌘ + R</Kbd>
<Kbd keys={["⇧", "⌘", "P"]} />
```

Place them right-aligned in menu rows and inside composer footers. Mono type, tertiary text — never accent.
