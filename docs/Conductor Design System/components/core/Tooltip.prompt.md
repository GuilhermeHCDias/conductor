Wraps a trigger and floats a glass chip beside it.

```jsx
<Tooltip content="Re-run last flow" shortcut="⌘R"><IconButton icon="refresh-cw" label="Re-run" /></Tooltip>
```

Tooltips explain glyph-only controls. If the label is already visible, don't add one.
