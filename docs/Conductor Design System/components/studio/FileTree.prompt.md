The project's flow files, right-hand rail.

```jsx
<FileTree nodes={tree} selectedId="teste" defaultExpanded={["maestro"]} onSelect={open} />
```

Rows are 28px, indent 14px per level, filenames mono. The selected row uses `--glass-selected` (accent tint), not a solid fill.
