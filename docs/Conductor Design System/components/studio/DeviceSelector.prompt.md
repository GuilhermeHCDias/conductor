Sits directly above the device mirror, full width of the mirror column.

```jsx
<DeviceSelector device="R9QYC01EMXL" platform="Android" state="connected" onClick={openPicker} />
```

Only Android is supported today — pass `platform="iOS"` solely in mockups that discuss future support.
